export const AZURE_SESSION_EXPIRED_GUIDANCE =
  "Azure CLI session expired. Open Azure sign-in again from the Query tab, complete the login, then run Reload again.";

export const AZURE_SESSION_EXPIRED_REASON =
  "Azure CLI session expired; Reload cannot run until Azure CLI has a fresh session.";

export const AZURE_SESSION_EXPIRED_NEXT_ACTION =
  "Open the Query tab, choose Sign in with Azure CLI, complete the Azure login, then run Reload again.";

export const AZURE_SESSION_EXPIRED_QUERY_HINT =
  "Azure CLI session expired. Use Sign in with Azure CLI to open Azure login again; Reload stays blocked until Azure CLI has a fresh session.";
